---
description: End-of-session summary for my Obsidian log
allowed-tools: Read, Bash(git diff:*), Bash(git log:*), Bash(git status)
---

Summarise this session for my project log. Output markdown into the chat — do not
write any files.

Write in German. Sections:

- **Gemacht** — what actually shipped, 3 to 5 bullets. Not what we discussed:
  what exists in the repo now that did not before.
- **Entschieden** — decisions taken and the reasoning. Flag any that deserve an ADR.
- **Offen** — what is unfinished, described precisely enough that I can resume
  cold in two weeks without rereading the diff.
- **Gelernt** — anything non-obvious about the Firefox APIs, TypeScript, or the
  problem domain that is worth a permanent note in my vault.

Be honest about what did not work or what we got wrong. A log that only records
successes is useless for spotting patterns later.
